DVX pytest plugin

dvc.testing.benchmarks
======================

Benchmark test definitions are part of ``dvc.testing``.

``dvc.testing.benchmarks`` structure:

* cli: tests for CLI commands

  * commands: granular tests for individual commands

* api: for python api only

  * methods: granular tests for individual methods (e.g. ``api.open/read``)
